<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SafePassword" parent="Theme.MaterialComponents">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/main_background</item>
    </style>

    <style name="Theme.SafePassword.SplashScreen" parent="Theme.MaterialComponents">
        <item name="android:background">@drawable/launch_background</item>
    </style>
</resources>